# Setup Checklist

Use this checklist after dependency installation:

- Runtime version matches project requirements.
- Dependency lockfile is respected when present.
- Local config exists and contains no real secret placeholders for commands being run.
- Build or import smoke test passes.
- Unit test command is known.
- Development server command is known for frontend or web apps.
- Any blocked external service is documented with the missing credential or process.
